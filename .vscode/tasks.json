{
    "version": "2.0.0",
    "tasks": [
      {
        "type": "shell",
        "label": "g++ build active file",
        "command": "C:/Mingw-w64/mingw32/bin/g++",
        "args": ["${file}", "-o", "${fileDirname}/${fileBasenameNoExtension}"],
        "options": {
          //"cwd": "/usr/bin"
        },
        "problemMatcher": ["$gcc"],
        "group": {
          "kind": "build",
          "isDefault": true
        }
      }
    ]
  }